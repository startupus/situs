import React from 'react';

const ProjectsFilters: React.FC = () => {
  return (
    <div className="mb-4">
      {/* Пустой компонент - кнопка создания проекта убрана, так как дублируется */}
    </div>
  );
};

export default ProjectsFilters;
